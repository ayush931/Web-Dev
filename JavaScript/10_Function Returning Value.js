// Function returning value

// Declaration

function passMessage () {
    // Function Body
    return "I am happy to learn Full Stack Web Development from PW Skills"
}

// Calling a function

let message = passMessage ()
// The function would be executed and on executing the return statement the value / expression are stored in the message variable.

console.log(message);

let newMessage = message + " Enroll Today"

console.log(newMessage);
