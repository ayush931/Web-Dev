// Ternary OP/ Ternary Condition
// condition ? True : False

let isLoggedIn = false
isLoggedIn ? console.log("Home Page") : console.log("Login");

let number = 0
number == 0 ? console.log(`The number is zero`) : number > 0 ? console.log(`The number is greater than zero`) : console.log(`The number is less than zero`);

let num1 = 9
let num2 = 5

num1 == num2 ? console.log(`Both the numbers are equal`) : num1 > num2 ? console.log(`The larger number among the two number is ${num1}`) : console.log(`The larger number among the two number is ${num2}`);